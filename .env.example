# Exemplo de variáveis de ambiente. Copie para .env e substitua pelos valores reais.
MONGODB_URI="mongodb+srv://<usuario>:<senha>@cluster0.mongodb.net/<seu_db>?retryWrites=true&w=majority"
# Opcional: nome do banco a ser usado (se diferente do especificado na URI)
# MONGODB_DBNAME=superagenda
